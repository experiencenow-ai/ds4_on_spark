diff --git a/ggml/src/ggml-cuda/ggml-cuda.cu b/ggml/src/ggml-cuda/ggml-cuda.cu
index c6160d04b..2ea0de11b 100644
--- a/ggml/src/ggml-cuda/ggml-cuda.cu
+++ b/ggml/src/ggml-cuda/ggml-cuda.cu
@@ -4840,25 +4840,7 @@ static bool ggml_backend_cuda_device_supports_op(ggml_backend_dev_t dev, const g
     }

     if (op->op == GGML_OP_FLASH_ATTN_EXT) {
-        bool result = ggml_cuda_flash_attn_ext_supported(dev_ctx->device, op);
-        if (!result) {
-            const ggml_tensor * Q = op->src[0];
-            const ggml_tensor * K = op->src[1];
-            const ggml_tensor * V = op->src[2];
-            const ggml_tensor * mask = op->src[3];
-            float max_bias = 0.0f;
-            memcpy(&max_bias, (const float *) op->op_params + 1, sizeof(float));
-            int gqa_ratio = Q->ne[2] / K->ne[2];
-            fprintf(stderr, "[cuda] REJECT %s: flash_attn unsupported Q[%ld,%ld,%ld,%ld] K[%ld,%ld,%ld,%ld] V[%ld,%ld,%ld,%ld] mask=%p max_bias=%f gqa=%d\n",
-                op->name,
-                Q->ne[0], Q->ne[1], Q->ne[2], Q->ne[3],
-                K->ne[0], K->ne[1], K->ne[2], K->ne[3],
-                V->ne[0], V->ne[1], V->ne[2], V->ne[3],
-                (void*)mask, max_bias, gqa_ratio);
-        } else {
-            fprintf(stderr, "[cuda] ACCEPT %s\n", op->name);
-        }
-        return result;
+        return ggml_cuda_flash_attn_ext_supported(dev_ctx->device, op);
     }

     switch (op->op) {
diff --git a/src/models/deepseek4.cpp b/src/models/deepseek4.cpp
index 452da9e98..4a9a67f8a 100644
--- a/src/models/deepseek4.cpp
+++ b/src/models/deepseek4.cpp
@@ -1158,6 +1158,15 @@ llm_build_deepseek4::llm_build_deepseek4(const llama_model & model, const llm_gr
                         n_tokens, n_tokens,
                         n_tokens, 0, hparams.n_swa, compress_ratio,
                         "dsv4_attn_raw_window_mask");
+                if (is_prefill && cparams.flash_attn && n_embd_head_k == 512) {
+                    const int64_t n_tokens_padded = GGML_PAD(n_tokens, 256);
+                    if (n_tokens_padded > n_tokens) {
+                        const int64_t n_pad = n_tokens_padded - n_tokens;
+                        k_all = ggml_pad(ctx0, kv, 0, 0, n_pad, 0);
+                        v_all = k_all;
+                        attn_mask = ggml_concat(ctx0, attn_mask, dsv4_new_filled_2d(ctx0, n_pad, n_tokens, -INFINITY), 0);
+                    }
+                }
             }

             if (!is_prefill) {
