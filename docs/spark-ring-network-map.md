# Spark Ring Network Map

The canonical Spark network map now lives at the repo root:

- `SPARKNETWORK.md`

Load that file first for current SSH targets, ring order, control-plane rules,
Centaur HTTP ports, writable roots, and the planned `sparknetworkd` API/service
contract. This page remains only as a compatibility pointer for older docs that
linked into `docs/`.
