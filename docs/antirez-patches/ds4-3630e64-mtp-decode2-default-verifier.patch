diff --git a/ds4.c b/ds4.c
index f0f8cc1..1aafe0d 100644
--- a/ds4.c
+++ b/ds4.c
@@ -13361,6 +13361,14 @@ static bool metal_graph_read_spec_logits_row(ds4_gpu_graph *g, uint32_t row, flo
                                  row_bytes) != 0;
 }

+typedef struct {
+    int target_eval_call_count;
+    int output_head_call_count;
+    int cuda_sync_count;
+    double target_eval_ms;
+    double output_head_ms;
+} ds4_mtp_decode2_stats;
+
 /* Exact N=2 target verifier for MTP.
  *
  * The generic batch prefill path is fast, but it is not a safe substitute for
@@ -13378,8 +13386,10 @@ static bool metal_graph_verify_decode2_exact(
         uint32_t               start,
         int                   *top0,
         float                 *logits0,
-        float                 *logits1) {
+        float                 *logits1,
+        ds4_mtp_decode2_stats *stats) {
     if (!g || !top0 || !logits1 || g->raw_cap == 0) return false;
+    if (stats) memset(stats, 0, sizeof(*stats));

     const uint64_t hc_dim = (uint64_t)DS4_N_HC * DS4_N_EMBD;
     ds4_gpu_tensor *cur0 = metal_graph_tensor_row_view(g->batch_cur_hc, 0, hc_dim);
@@ -13409,6 +13419,7 @@ static bool metal_graph_verify_decode2_exact(
     ds4_gpu_tensor *saved_after = g->after_ffn_hc;
     const bool saved_capture = g->spec_capture_prefix1;
     g->spec_capture_prefix1 = true;
+    const double target_t0 = stats ? now_sec() : 0.0;
     if (ok) ok = ds4_gpu_begin_commands() != 0;
     for (uint32_t il = 0; ok && il < DS4_N_LAYER; il++) {
         const uint32_t pos0 = start;
@@ -13449,12 +13460,20 @@ static bool metal_graph_verify_decode2_exact(
         tmp = cur1; cur1 = next1; next1 = tmp;
     }
     if (ok) ok = ds4_gpu_end_commands() != 0;
-    else (void)ds4_gpu_synchronize();
+    else {
+        (void)ds4_gpu_synchronize();
+        if (stats) stats->cuda_sync_count++;
+    }
+    if (stats) {
+        stats->target_eval_call_count += 2;
+        stats->target_eval_ms += (now_sec() - target_t0) * 1000.0;
+    }
     g->spec_capture_prefix1 = saved_capture;
     g->cur_hc = saved_cur;
     g->after_ffn_hc = saved_after;

     if (ok) {
+        const double head_t0 = stats ? now_sec() : 0.0;
         g->cur_hc = cur0;
         ok = ds4_gpu_begin_commands() != 0;
         if (ok) ok = metal_graph_encode_output_head(g, model, weights, weights->output->dim[1]);
@@ -13464,7 +13483,14 @@ static bool metal_graph_verify_decode2_exact(
                                                    1,
                                                    1) != 0;
         if (ok) ok = ds4_gpu_end_commands() != 0;
-        else (void)ds4_gpu_synchronize();
+        else {
+            (void)ds4_gpu_synchronize();
+            if (stats) stats->cuda_sync_count++;
+        }
+        if (stats) {
+            stats->output_head_call_count++;
+            stats->output_head_ms += (now_sec() - head_t0) * 1000.0;
+        }
         g->cur_hc = saved_cur;
         if (ok) ok = ds4_gpu_tensor_read(g->comp_selected, 0, top0, sizeof(*top0)) != 0;
         if (ok && logits0) {
@@ -13476,11 +13502,19 @@ static bool metal_graph_verify_decode2_exact(
     }

     if (ok) {
+        const double head_t0 = stats ? now_sec() : 0.0;
         g->cur_hc = cur1;
         ok = ds4_gpu_begin_commands() != 0;
         if (ok) ok = metal_graph_encode_output_head(g, model, weights, weights->output->dim[1]);
         if (ok) ok = ds4_gpu_end_commands() != 0;
-        else (void)ds4_gpu_synchronize();
+        else {
+            (void)ds4_gpu_synchronize();
+            if (stats) stats->cuda_sync_count++;
+        }
+        if (stats) {
+            stats->output_head_call_count++;
+            stats->output_head_ms += (now_sec() - head_t0) * 1000.0;
+        }
         g->cur_hc = saved_cur;
         if (ok) {
             ok = ds4_gpu_tensor_read(g->logits,
@@ -17291,20 +17325,19 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
     }

     /*
-     * The useful N=2 verifier is the tiny batch path: it verifies two target
-     * positions in one layer-major pass and commits prefix-1 directly on a
-     * partial accept.  Like the rest of the non-quality Metal path, it may pick
-     * a different greedy token when batched reductions perturb nearly-tied
-     * logits.  --quality / DS4_MTP_STRICT selects the exact decode verifier,
-     * which preserves the one-token target stream but is not a speed win.
+     * The useful N=2 verifier is the exact decode2 path: it preserves the
+     * one-token target stream while avoiding the generic suffix batch verifier
+     * that is slower than target-only decode on CUDA/Metal.  DS4_MTP_BATCH_VERIFY
+     * keeps the older batch verifier available for A/B measurement.
      */
     const bool use_decode2_exact =
-        draft_n == 2 && strict_mtp && getenv("DS4_MTP_BATCH_VERIFY") == NULL;
+        draft_n == 2 && getenv("DS4_MTP_BATCH_VERIFY") == NULL;
     if (use_decode2_exact) {
         ds4_spec_frontier frontier;
         memset(&frontier, 0, sizeof(frontier));
         float *row_logits = xmalloc((size_t)DS4_N_VOCAB * sizeof(row_logits[0]));
-        float *row0_logits = xmalloc((size_t)DS4_N_VOCAB * sizeof(row0_logits[0]));
+        ds4_mtp_decode2_stats decode2_stats;
+        memset(&decode2_stats, 0, sizeof(decode2_stats));
         const int start = s->checkpoint.len;
         int row0_top = -1;
         const double snapshot_t0 = mtp_timing ? now_sec() : 0.0;
@@ -17319,11 +17352,23 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
                                                   drafts[1],
                                                   (uint32_t)start,
                                                   &row0_top,
-                                                  row0_logits,
-                                                  row_logits);
+                                                  NULL,
+                                                  row_logits,
+                                                  &decode2_stats);
         }
         const double verify_done = mtp_timing ? now_sec() : 0.0;
         if (ok && row0_top == drafts[1]) {
+            if (mtp_conf_log) {
+                fprintf(stderr,
+                        "ds4: mtp conf drafted=%d committed=%d mtp_top=%d runner=%d margin=%.6f target_next=%d draft_next=%d\n",
+                        2,
+                        2,
+                        mtp_last_top0,
+                        mtp_last_top1,
+                        mtp_last_margin,
+                        row0_top,
+                        drafts[1]);
+            }
             memcpy(s->logits, row_logits, (size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
             token_vec_push(&s->checkpoint, drafts[0]);
             token_vec_push(&s->checkpoint, drafts[1]);
@@ -17334,23 +17379,48 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
             DS4_MTP_KEEP_ACCEPTED(2);
             if (mtp_timing) {
                 fprintf(stderr,
-                        "ds4: mtp timing decode2 drafted=2 committed=2 draft=%.3f ms snapshot=%.3f ms verify=%.3f ms total=%.3f ms\n",
+                        "ds4: mtp timing decode2 drafted=2 committed=2 draft=%.3f ms snapshot=%.3f ms verify=%.3f ms target=%.3f ms head=%.3f ms target_calls=%d head_calls=%d draft_calls=1 replay_calls=0 rewind_calls=0 cache_sync_calls=0 cuda_sync_calls=%d emitted=3 total=%.3f ms\n",
                         (mtp_t_after_draft - mtp_t0) * 1000.0,
                         (snapshot_done - snapshot_t0) * 1000.0,
                         (verify_done - snapshot_done) * 1000.0,
+                        decode2_stats.target_eval_ms,
+                        decode2_stats.output_head_ms,
+                        decode2_stats.target_eval_call_count,
+                        decode2_stats.output_head_call_count,
+                        decode2_stats.cuda_sync_count,
                         (now_sec() - mtp_t0) * 1000.0);
             }
             spec_frontier_free(&frontier);
-            free(row0_logits);
             free(row_logits);
             return n_accept;
         }

         if (ok) {
+            if (mtp_conf_log) {
+                fprintf(stderr,
+                        "ds4: mtp conf drafted=%d committed=%d mtp_top=%d runner=%d margin=%.6f target_next=%d draft_next=%d\n",
+                        2,
+                        1,
+                        mtp_last_top0,
+                        mtp_last_top1,
+                        mtp_last_margin,
+                        row0_top,
+                        drafts[1]);
+            }
             s->checkpoint.len = start;
-            ok = spec_frontier_commit_prefix1(s);
+            ok = have_frontier && spec_frontier_restore(&frontier, s);
+        }
+        const double replay_t0 = mtp_timing ? now_sec() : 0.0;
+        if (ok) {
+            ok = metal_graph_eval_token_raw_swa(&s->graph,
+                                                &e->model,
+                                                &e->weights,
+                                                drafts[0],
+                                                (uint32_t)start,
+                                                row_logits);
         }
-        if (ok) memcpy(s->logits, row0_logits, (size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
+        const double replay_done = mtp_timing ? now_sec() : 0.0;
+        if (ok) memcpy(s->logits, row_logits, (size_t)DS4_N_VOCAB * sizeof(s->logits[0]));
         if (ok) {
             token_vec_push(&s->checkpoint, drafts[0]);
             accepted[n_accept++] = drafts[0];
@@ -17358,17 +17428,20 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
             s->mtp_draft_valid = false;
             DS4_MTP_KEEP_ACCEPTED(1);
             if (mtp_timing) {
-                const double replay_done = now_sec();
                 fprintf(stderr,
-                        "ds4: mtp timing decode2 drafted=2 committed=1 draft=%.3f ms snapshot=%.3f ms verify=%.3f ms prefix=%.3f ms total=%.3f ms\n",
+                        "ds4: mtp timing decode2 drafted=2 committed=1 draft=%.3f ms snapshot=%.3f ms verify=%.3f ms target=%.3f ms head=%.3f ms target_calls=%d head_calls=%d draft_calls=1 replay_calls=1 rewind_calls=1 cache_sync_calls=0 cuda_sync_calls=%d replay=%.3f ms total=%.3f ms\n",
                         (mtp_t_after_draft - mtp_t0) * 1000.0,
                         (snapshot_done - snapshot_t0) * 1000.0,
                         (verify_done - snapshot_done) * 1000.0,
-                        (replay_done - verify_done) * 1000.0,
+                        decode2_stats.target_eval_ms,
+                        decode2_stats.output_head_ms,
+                        decode2_stats.target_eval_call_count,
+                        decode2_stats.output_head_call_count,
+                        decode2_stats.cuda_sync_count,
+                        (replay_done - replay_t0) * 1000.0,
                         (replay_done - mtp_t0) * 1000.0);
             }
             spec_frontier_free(&frontier);
-            free(row0_logits);
             free(row_logits);
             return n_accept;
         }
@@ -17377,7 +17450,6 @@ int ds4_session_eval_speculative_argmax(ds4_session *s, int first_token,
             (void)spec_frontier_restore(&frontier, s);
         }
         spec_frontier_free(&frontier);
-        free(row0_logits);
         free(row_logits);
         if (getenv("DS4_MTP_SPEC_LOG")) {
             fprintf(stderr, "ds4: mtp decode2 verifier failed, falling back to sequential\n");
